# Save exact versions in package.json (no ^ or ~ prefixes)
save-exact=true

# Install exact versions from package-lock.json
package-lock=true

# Prevent automatic updates to minor/patch versions
save-prefix=